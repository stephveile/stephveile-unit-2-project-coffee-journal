export default class UserDTO {
    constructor(userName, userEmail, userPassword, userCity) {
        this.userName = userName;
        this.userEmail = userEmail;
        this.userPassword = userPassword;
        this.userCity = userCity;
    }
}
