export default class User {
    constructor(id, userName, userEmail, userPassword, userCity) {
        this.id = id;
        this.userName = userName;
        this.userEmail = userEmail;
        this.userPassword = userPassword;
        this.userCity = userCity;
    }
}
